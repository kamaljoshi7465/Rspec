require 'rails_helper'

RSpec.describe Article, type: :model do
    # before(:all) do
    #     @article = Article.create(title: "kamal joshi", body: "Hellow Guysssssssss!")
    # end
    # it 'checks that a article can be created' do
    #     expect(@article) == be_valid?
    # end
    # it 'checks that a article can be read' do
    #     expect(Article.find_by_title("kamal joshi")) == (@article)
    # end
    # it 'checks that a article can be updated' do
    #     @article.update(:title => "kamal joshi")
    #     expect(Article.find_by_title("kamal joshi")) == (@article)
    # end
    # it 'checks that a article can be destroyed' do
    #     @article.destroy
    #     # expect(Article.count) == (0)
    #     expect(Article.find_by(title: "Like a Rolling Stone - Remastered")).to be_nil
    # end
end